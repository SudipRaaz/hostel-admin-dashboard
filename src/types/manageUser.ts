export interface UserManagementUserListType {
    name: string;
    email: string;
    gender: string;
    phone_number: string;
    address: string;
    date_of_birth: string;
    is_active: boolean,
    admissionDate: string; 
    seatID: number
}